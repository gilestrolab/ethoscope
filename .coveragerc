# Coverage configuration for the Ethoscope project
[run]
source = src/ethoscope/ethoscope, src/node/ethoscope_node
omit =
    */tests/*
    */test_*
    */conftest.py
    */setup.py
    */venv/*
    */env/*
    */__pycache__/*
    */build/*
    */dist/*
    */.*
    */static/*
    */scripts/*

[report]
exclude_lines =
    pragma: no cover
    def __repr__
    if self.debug:
    if settings.DEBUG
    raise AssertionError
    raise NotImplementedError
    if 0:
    if __name__ == .__main__.:
    class .*\bProtocol\):
    @(abc\.)?abstractmethod

# Fail if coverage falls below this threshold
# Current coverage: 42.60% after scheduler and tracker improvements
# Progress: 25.18% (CI) → 32.48% (session) → 42.60% (current, +10.12 overall)
# Unit tests only: ~31-32% | All tests: 42.60%
# Milestone: 40% ✓ | Target: 50% (need +7.40 points, ~520 lines)
# Recent: diagnostics 98.96%, scheduler 99.44%, trackers 96.61%
fail_under = 42

ignore_errors = True
show_missing = True
precision = 2

[html]
directory = htmlcov
title = Ethoscope Test Coverage Report

[xml]
output = coverage.xml
