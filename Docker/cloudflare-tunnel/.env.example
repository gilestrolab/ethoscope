# Cloudflare Tunnel Environment Configuration
# Copy this file to .env and fill in your specific values

# REQUIRED: Cloudflare Tunnel Token
# Get this from Cloudflare Zero Trust Dashboard -> Access -> Tunnels
# Create a new tunnel and copy the token provided
TUNNEL_TOKEN=your_cloudflare_tunnel_token_here

# OPTIONAL: Node ID for the tunnel
# If set to "auto", will use hostname
# Otherwise, specify a unique identifier (e.g., "beckwith-node", "lab-node-01")
# This will become the subdomain: {NODE_ID}.ethoscope.net
NODE_ID=auto

# OPTIONAL: Additional configuration
# These are handled by the main ethoscope configuration system
# TUNNEL_DOMAIN=ethoscope.net
# TUNNEL_ENABLED=false